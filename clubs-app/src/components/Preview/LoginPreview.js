import React from 'react';
import preview from '../../assets/Authpage.png'

const LoginPreview = (props) => {


    //non members - see apply and the top div
    return (
        <div>
            <img src={preview} alt="" />
        </div>
    )
};

export default LoginPreview;